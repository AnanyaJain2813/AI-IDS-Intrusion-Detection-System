services:
  - type: web
    name: sentry-threat-detection
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python startup.py && gunicorn wsgi:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
